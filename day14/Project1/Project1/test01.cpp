/*
  char ary[] = {'a', 'b', 'c', 'd', 'e', 'f'};
  조건: while문과 if문만으로 배열 순서를 바꾸어 저장 출력하시오.
*/

#include <iostream>

using namespace std;

int main()
{
	char ary[] = { 'a', 'b', 'c', 'd', 'e', 'f' };
	int size = sizeof(ary) / sizeof(ary[0]);
	int start, end;

	return 0;
}